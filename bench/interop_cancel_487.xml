<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- RFC 3261 Section 9.1 / 15.1.2: CANCEL and 487 behavior.
     UAC sends INVITE, waits for 100 Trying, then sends CANCEL.
     Proxy forwards CANCEL to UAS.
     Expect 200 OK for CANCEL and 487 Request Terminated for INVITE.
     UAC ACKs the 487. -->

<scenario name="Interop: CANCEL produces 487">

  <!-- UAC sends INVITE -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:cancel@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch-1]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: cancel <sip:cancel@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Interop CANCEL-487 test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=bench 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000

    ]]>
  </send>

  <!-- Wait for 100 Trying before sending CANCEL (RFC 3261 9.1) -->
  <recv response="100" />

  <!-- Optional 180 may arrive before our CANCEL -->
  <recv response="180" optional="true" />

  <!-- Send CANCEL with same branch as INVITE -->
  <send retrans="500">
    <![CDATA[

      CANCEL sip:cancel@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch-1]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: cancel <sip:cancel@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 CANCEL
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- 200 OK for the CANCEL -->
  <recv response="200" />

  <!-- 487 Request Terminated for the INVITE -->
  <recv response="487" />

  <!-- ACK the 487 -->
  <send>
    <![CDATA[

      ACK sip:cancel@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch-1]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: cancel <sip:cancel@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>
</scenario>
