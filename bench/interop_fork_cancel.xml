<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- RFC 3261 Section 16.7: Forking proxy behavior.
     UAC sends INVITE, proxy forks to two targets.
     First target rings (180), second answers (200).
     Proxy cancels first target and forwards 200.
     UAC completes with ACK then BYE. -->

<scenario name="Interop: fork and cancel losing branch">

  <!-- UAC sends INVITE through the proxy -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:forked@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: forked <sip:forked@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Interop fork-cancel test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=bench 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000

    ]]>
  </send>

  <!-- Expect 100 Trying from proxy -->
  <recv response="100" optional="true" />

  <!-- Expect 180 Ringing forwarded from first target -->
  <recv response="180" optional="true" />

  <!-- Expect 200 OK forwarded from winning target -->
  <recv response="200" rtd="true">
    <action>
      <ereg regexp="To:.*tag=([^;]*)" search_in="msg" header="To:" assign_to="remote_tag" />
    </action>
  </recv>

  <!-- ACK the 200 -->
  <send>
    <![CDATA[

      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: forked <sip:forked@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <pause milliseconds="1000" />

  <!-- BYE to tear down the dialog -->
  <send retrans="500">
    <![CDATA[

      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: forked <sip:forked@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true" />
</scenario>
