<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- RFC 3262: Reliable provisional responses (100rel / PRACK).
     UAC sends INVITE with Supported: 100rel.
     UAS responds 183 with Require: 100rel and RSeq.
     UAC sends PRACK with RAck matching RSeq.
     Proxy forwards PRACK via dialog binding.
     UAS responds 200 to PRACK, then 200 to INVITE. -->

<scenario name="Interop: PRACK reliable provisional">

  <!-- UAC sends INVITE with 100rel support -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:prack@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: prack <sip:prack@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Supported: 100rel
      Max-Forwards: 70
      Subject: Interop PRACK test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=bench 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000

    ]]>
  </send>

  <recv response="100" optional="true" />

  <!-- Receive 183 Session Progress with RSeq -->
  <recv response="183" rtd="true">
    <action>
      <ereg regexp="RSeq: ([0-9]+)" search_in="msg" assign_to="rseq_val" />
    </action>
  </recv>

  <!-- Send PRACK with RAck: RSeq CSeq Method -->
  <send retrans="500">
    <![CDATA[

      PRACK sip:prack@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: prack <sip:prack@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 PRACK
      RAck: [$rseq_val] 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!-- 200 OK for PRACK -->
  <recv response="200" />

  <!-- 200 OK for INVITE -->
  <recv response="200" />

  <!-- ACK the INVITE 200 -->
  <send>
    <![CDATA[

      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: prack <sip:prack@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <pause milliseconds="500" />

  <!-- BYE -->
  <send retrans="500">
    <![CDATA[

      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: prack <sip:prack@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 3 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" crlf="true" />
</scenario>
